extends layout

block content
  h1= title

  ul
    each roaster in roaster_list
      li
        a(href=`/catalog/roasters/`+roaster.id) #{roaster.name}
        p #{roaster.address} | #{roaster.phone}

    else
      li There are no Roasters.
