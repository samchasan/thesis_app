DWD

notes of process.env
---
Different programs run as 'processes' in the background controlling 
the operating system.

Starts in context - process & environment (a set of configuration variables)
Can add environment variables in command line WORD=VARIABLE for single program run
Or you can have it run for any program run in the same window by exporting
can view environment variables by typing env in terminal

---

links to maker spaces or other places to foster community growth!

----

use scss (sass) to create variables for css 

create custom user checking middle ware for express so I don't need to have 
everything in each page
